---
phase: 02.5
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/kay-core/src/forge_domain/mod.rs
  - crates/kay-core/src/forge_api/mod.rs
  - crates/kay-core/src/forge_app/mod.rs
  - crates/kay-core/src/forge_main/mod.rs
  - shell-plugin/forge.theme.zsh
  - shell-plugin/doctor.zsh
  - shell-plugin/keyboard.zsh
  - shell-plugin/forge.setup.zsh
  - templates/forge-partial-tool-error-reflection.md
  - commands/github-pr-description.md
  - vertex.json
autonomous: true
requirements: [WS-05]
must_haves:
  truths:
    - "git status shows a clean working tree after the revert commit lands"
    - "crates/kay-core/src/ contains 23 forge_*/lib.rs files (not mod.rs) — the Phase 1 import state is restored"
    - "shell-plugin/, templates/, commands/, vertex.json exist at repo root"
    - "crates/forge_config/.forge.toml does NOT exist yet (placed by plan 02 when that sub-crate is created)"
  artifacts:
    - path: "shell-plugin/forge.theme.zsh"
      provides: "ZSH plugin — required by forge_main include_str! path ../../../../shell-plugin/forge.theme.zsh"
    - path: "shell-plugin/doctor.zsh"
      provides: "ZSH doctor script — required by forge_main include_str!"
    - path: "shell-plugin/keyboard.zsh"
      provides: "ZSH keyboard script — required by forge_main include_str!"
    - path: "shell-plugin/forge.setup.zsh"
      provides: "ZSH setup script — required by forge_main include_str!"
    - path: "templates/forge-partial-tool-error-reflection.md"
      provides: "Tool error reflection template — required by forge_domain include_str! path ../../../../templates/..."
    - path: "commands/github-pr-description.md"
      provides: "GitHub PR description command — required by forge_services include_str! path ../../../commands/..."
    - path: "vertex.json"
      provides: "Vertex AI config — required by forge_repo concat!(env!(CARGO_MANIFEST_DIR), /../../vertex.json)"
  key_links:
    - from: "forge_main/src/zsh/plugin.rs"
      to: "shell-plugin/forge.theme.zsh"
      via: "include_str!(../../../../shell-plugin/forge.theme.zsh)"
      pattern: "shell-plugin/forge.theme.zsh"
    - from: "forge_domain/src/tools/result.rs"
      to: "templates/forge-partial-tool-error-reflection.md"
      via: "include_str!(../../../../templates/forge-partial-tool-error-reflection.md)"
      pattern: "templates/forge-partial-tool-error-reflection.md"
---

<objective>
Restore the working tree to the Phase 1 import state by reverting all 24 source-change commits from plans 02-02..02-05, then fetch the missing resource files (shell-plugin/, templates/, commands/, vertex.json) from ForgeCode upstream at the canonical SHA.

Purpose: The 24 path-rewrite commits made `use crate::forge_X::Y` correct for a mono-crate layout. Under the sub-crate split (D-01 Option c), each sub-crate's `use crate::X` is already correct relative to its own crate root — so every rewrite must be undone. The 4 missing resource directories caused include_str! compile failures in the mono-crate; they must exist at repo root before any sub-crate is created.

Output: One DCO-signed revert commit + fetched resource files committed separately. Working tree clean. crates/kay-core/src/ has 23 forge_*/lib.rs files (original Phase 1 import state).
</objective>

<execution_context>
@/Users/shafqat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shafqat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.5-kay-core-sub-crate-split/02.5-CONTEXT.md
@.planning/phases/02.5-kay-core-sub-crate-split/02.5-RESEARCH.md
</context>

<interfaces>
<!-- Current state of the working tree at plan start (HEAD = dc6c9e0): -->
<!-- 132 uncommitted files under crates/kay-core/src/ (obsolete extended-use rewrites from /tmp/rewrite-indented-02-05.sh) -->
<!-- 24 source-change commits on HEAD:
  3d85520 refactor(02-05): rewrite imports for forge_main
  57045a4 refactor(02-05): rewrite imports for forge_api
  4991060 refactor(02-05): rewrite imports for forge_repo
  a6f37f7 refactor(02-05): rewrite imports for forge_infra
  404ff21 refactor(02-05): rewrite imports for forge_services
  808edcc refactor(02-04): rewrite imports for forge_app
  674f0d0 refactor(02-03): path-rewrite forge_snaps (sub-wave C)
  e1bdad3 refactor(02-03): path-rewrite forge_tracker (sub-wave C)
  3147961 refactor(02-03): path-rewrite forge_spinner (sub-wave C)
  7c5c1d7 refactor(02-03): path-rewrite forge_fs (sub-wave C)
  88f9ad3 refactor(02-03): path-rewrite forge_domain (sub-wave B)
  3587c7d refactor(02-03): path-rewrite forge_markdown_stream (sub-wave A)
  ddc5fa7 refactor(02-03): path-rewrite forge_select (sub-wave A)
  fb23dbf refactor(02-03): path-rewrite forge_display (sub-wave A)
  594b545 refactor(02-03): path-rewrite forge_config (sub-wave A)
  9d012b0 refactor(02-03): path-rewrite forge_ci (sub-wave A)
  f77d0ab refactor(02-03): path-rewrite forge_embed (sub-wave A)
  43d5c8b refactor(02-03): path-rewrite forge_test_kit (sub-wave A)
  5ed3ac7 refactor(02-03): path-rewrite forge_walker (sub-wave A)
  a01a1c1 refactor(02-03): path-rewrite forge_tool_macros (sub-wave A)
  a07fdf0 refactor(02-03): path-rewrite forge_template (sub-wave A)
  e3f8f73 refactor(02-03): path-rewrite forge_stream (sub-wave A)
  9c515a7 refactor(02-03): path-rewrite forge_json_repair (sub-wave A)
  bb57694 refactor(02-02): rename 23 forge_*/lib.rs to mod.rs
-->
<!-- After revert: every forge_*/mod.rs becomes forge_*/lib.rs again; every use-path rewrite is undone -->
<!-- Resource files to fetch from ForgeCode upstream SHA 022ecd994eaec30b519b13348c64ef314f825e21: -->
<!-- shell-plugin/forge.theme.zsh, shell-plugin/doctor.zsh, shell-plugin/keyboard.zsh, shell-plugin/forge.setup.zsh -->
<!-- shell-plugin/lib/ directory (many files — use git archive or loop through known list) -->
<!-- templates/forge-partial-tool-error-reflection.md -->
<!-- commands/github-pr-description.md -->
<!-- vertex.json -->
<!-- NOTE: crates/forge_config/.forge.toml is fetched in plan 02 when forge_config sub-crate dir is created -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Discard uncommitted files and revert 24 source-change commits</name>
  <files>
    crates/kay-core/src/forge_api/lib.rs,
    crates/kay-core/src/forge_app/lib.rs,
    crates/kay-core/src/forge_ci/lib.rs,
    crates/kay-core/src/forge_config/lib.rs,
    crates/kay-core/src/forge_display/lib.rs,
    crates/kay-core/src/forge_domain/lib.rs,
    crates/kay-core/src/forge_embed/lib.rs,
    crates/kay-core/src/forge_fs/lib.rs,
    crates/kay-core/src/forge_infra/lib.rs,
    crates/kay-core/src/forge_json_repair/lib.rs,
    crates/kay-core/src/forge_main/lib.rs,
    crates/kay-core/src/forge_markdown_stream/lib.rs,
    crates/kay-core/src/forge_repo/lib.rs,
    crates/kay-core/src/forge_select/lib.rs,
    crates/kay-core/src/forge_services/lib.rs,
    crates/kay-core/src/forge_snaps/lib.rs,
    crates/kay-core/src/forge_spinner/lib.rs,
    crates/kay-core/src/forge_stream/lib.rs,
    crates/kay-core/src/forge_template/lib.rs,
    crates/kay-core/src/forge_test_kit/lib.rs,
    crates/kay-core/src/forge_tool_macros/lib.rs,
    crates/kay-core/src/forge_tracker/lib.rs,
    crates/kay-core/src/forge_walker/lib.rs
  </files>
  <read_first>
    - crates/kay-core/src/lib.rs (current state — must verify content before git operations)
    - .planning/phases/02.5-kay-core-sub-crate-split/02.5-RESEARCH.md §4 and §5 (cleanup strategy and commit revert list)
  </read_first>
  <action>
STEP 1 — Discard 132 uncommitted files (extended indented-use rewrites from /tmp/rewrite-indented-02-05.sh; obsolete under sub-crate split):

    git status --short   # verify all changed files are under crates/kay-core/src/
    git restore .        # reset working tree to HEAD (dc6c9e0)
    git status --short   # must show empty output (clean working tree)

STEP 2 — Batch revert all 24 source-change commits in one operation (newest-first = correct revert order):

    git revert --no-commit \
      3d85520 57045a4 4991060 a6f37f7 404ff21 808edcc \
      674f0d0 e1bdad3 3147961 7c5c1d7 88f9ad3 3587c7d \
      ddc5fa7 fb23dbf 594b545 9d012b0 f77d0ab 43d5c8b \
      5ed3ac7 a01a1c1 a07fdf0 e3f8f73 9c515a7 bb57694

IMPORTANT: The SHA list above is EXACTLY as provided in RESEARCH.md §5. Do not reorder.

STEP 3 — Review staged diff to confirm correctness:
    git diff --cached --stat

Expected: Every forge_*/mod.rs listed in the diff should be renamed back to forge_*/lib.rs (bb57694 revert). All use-path rewrites should be undone.

STEP 4 — Commit as single DCO-signed revert:

    git commit -s -m "refactor(02.5): revert plans 02-02..02-05 rewrites — sub-crate split supersedes mono-crate path rewrites

D-01 Option (c) sub-crate split makes the mono-crate path rewrites
from plans 02-02 through 02-05 incorrect. Each sub-crate's use paths
are correct as-is per the original ForgeCode source because crate now
refers to each sub-crate itself.

Reverts: bb57694 9c515a7 e3f8f73 a07fdf0 a01a1c1 5ed3ac7 43d5c8b f77d0ab
         9d012b0 594b545 fb23dbf ddc5fa7 3587c7d 88f9ad3 7c5c1d7 3147961
         e1bdad3 674f0d0 808edcc 404ff21 a6f37f7 4991060 57045a4 3d85520

Refs: WS-05

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
  </action>
  <verify>
    <automated>
      # Verify working tree is clean
      git status --short  # must output nothing

      # Verify 23 lib.rs files exist (not mod.rs) under forge_* subdirs
      find crates/kay-core/src -name 'lib.rs' -path '*/forge_*/*' | wc -l
      # Expected: 23

      # Verify no mod.rs files remain as crate roots
      find crates/kay-core/src -maxdepth 2 -name 'mod.rs' | wc -l
      # Expected: 0 (mod.rs files deeper in tree are fine; only top-level forge_*/mod.rs was renamed)
    </automated>
  </verify>
  <acceptance_criteria>
    - `git status --short` outputs nothing (clean working tree)
    - `find crates/kay-core/src -name 'lib.rs' -path '*/forge_*/*' | wc -l` = 23
    - `git log --oneline HEAD~1..HEAD` shows exactly one commit starting with "refactor(02.5): revert"
    - That commit carries `Signed-off-by: Shafqat Ullah <shafqat@sourcevo.com>` (DCO)
  </acceptance_criteria>
  <done>Working tree clean; 23 forge_*/lib.rs files restored to Phase 1 import state; single DCO-signed revert commit on HEAD.</done>
</task>

<task type="auto">
  <name>Task 2: Fetch missing resource files from ForgeCode upstream SHA</name>
  <files>
    shell-plugin/forge.theme.zsh,
    shell-plugin/doctor.zsh,
    shell-plugin/keyboard.zsh,
    shell-plugin/forge.setup.zsh,
    templates/forge-partial-tool-error-reflection.md,
    commands/github-pr-description.md,
    vertex.json
  </files>
  <read_first>
    - .planning/phases/02.5-kay-core-sub-crate-split/02.5-RESEARCH.md §2 (include_str! resource file inventory — MISSING files table)
    - .planning/phases/02.5-kay-core-sub-crate-split/02.5-RESEARCH.md §2 (path resolution math confirming repo-root placement)
  </read_first>
  <action>
The upstream SHA is: 022ecd994eaec30b519b13348c64ef314f825e21
Base URL for raw fetch: https://raw.githubusercontent.com/antinomyhq/forgecode/022ecd994eaec30b519b13348c64ef314f825e21

Fetch each file with curl. Create parent directories before fetching. SECURITY: verify the SHA after download by checking the Content matches what is expected (no integrity failure triggers).

STEP 1 — Create directories:
    mkdir -p shell-plugin/lib templates commands

STEP 2 — Fetch individual files:
    curl -fsSL "https://raw.githubusercontent.com/antinomyhq/forgecode/022ecd994eaec30b519b13348c64ef314f825e21/shell-plugin/forge.theme.zsh" -o shell-plugin/forge.theme.zsh
    curl -fsSL "https://raw.githubusercontent.com/antinomyhq/forgecode/022ecd994eaec30b519b13348c64ef314f825e21/shell-plugin/doctor.zsh" -o shell-plugin/doctor.zsh
    curl -fsSL "https://raw.githubusercontent.com/antinomyhq/forgecode/022ecd994eaec30b519b13348c64ef314f825e21/shell-plugin/keyboard.zsh" -o shell-plugin/keyboard.zsh
    curl -fsSL "https://raw.githubusercontent.com/antinomyhq/forgecode/022ecd994eaec30b519b13348c64ef314f825e21/shell-plugin/forge.setup.zsh" -o shell-plugin/forge.setup.zsh
    curl -fsSL "https://raw.githubusercontent.com/antinomyhq/forgecode/022ecd994eaec30b519b13348c64ef314f825e21/templates/forge-partial-tool-error-reflection.md" -o templates/forge-partial-tool-error-reflection.md
    curl -fsSL "https://raw.githubusercontent.com/antinomyhq/forgecode/022ecd994eaec30b519b13348c64ef314f825e21/commands/github-pr-description.md" -o commands/github-pr-description.md
    curl -fsSL "https://raw.githubusercontent.com/antinomyhq/forgecode/022ecd994eaec30b519b13348c64ef314f825e21/vertex.json" -o vertex.json

STEP 3 — Fetch shell-plugin/lib/ directory contents.
Use the GitHub API to list the directory, then fetch each file. The canonical approach:
    # List files in shell-plugin/lib/ at the upstream SHA
    curl -fsSL "https://api.github.com/repos/antinomyhq/forgecode/contents/shell-plugin/lib?ref=022ecd994eaec30b519b13348c64ef314f825e21" \
      | grep '"download_url"' | sed 's/.*"download_url": "\([^"]*\)".*/\1/' | while read url; do
        fname=$(basename "$url")
        curl -fsSL "$url" -o "shell-plugin/lib/$fname"
      done

STEP 4 — Verify all files exist and are non-empty:
    ls -la shell-plugin/forge.theme.zsh shell-plugin/doctor.zsh shell-plugin/keyboard.zsh shell-plugin/forge.setup.zsh
    ls -la templates/forge-partial-tool-error-reflection.md commands/github-pr-description.md vertex.json
    ls -la shell-plugin/lib/

STEP 5 — Commit resource files with DCO signoff:
    git add shell-plugin/ templates/ commands/ vertex.json
    git commit -s -m "chore(02.5): fetch ForgeCode resource files from upstream SHA 022ecd9

Missing files required by include_str!() calls in forge_main, forge_services,
forge_domain, and forge_repo. These existed at ForgeCode repo root but were
not imported in Phase 1 (01-03 only copied crates/*/src/ content).

Files fetched from: https://github.com/antinomyhq/forgecode
Upstream SHA: 022ecd994eaec30b519b13348c64ef314f825e21

T-2.5-01 (supply-chain): SHA verified via GitHub raw URL pinned to exact commit.

Refs: WS-05

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"

NOTE: Do NOT fetch crates/forge_config/.forge.toml here. That file goes to
crates/forge_config/.forge.toml which is created when the forge_config sub-crate
directory is set up in plan 02. Fetch it during Task 1 of plan 02.
  </action>
  <verify>
    <automated>
      ls shell-plugin/forge.theme.zsh shell-plugin/doctor.zsh shell-plugin/keyboard.zsh shell-plugin/forge.setup.zsh
      ls templates/forge-partial-tool-error-reflection.md
      ls commands/github-pr-description.md
      ls vertex.json
      ls shell-plugin/lib/
      git log --oneline HEAD~1..HEAD
      # Must show commit starting with "chore(02.5): fetch ForgeCode resource files"
    </automated>
  </verify>
  <acceptance_criteria>
    - `ls shell-plugin/forge.theme.zsh` exits 0 and file is non-empty (size > 0)
    - `ls shell-plugin/doctor.zsh` exits 0 and file is non-empty
    - `ls shell-plugin/keyboard.zsh` exits 0 and file is non-empty
    - `ls shell-plugin/forge.setup.zsh` exits 0 and file is non-empty
    - `ls shell-plugin/lib/` exits 0 and directory contains at least 1 file
    - `ls templates/forge-partial-tool-error-reflection.md` exits 0 and file is non-empty
    - `ls commands/github-pr-description.md` exits 0 and file is non-empty
    - `ls vertex.json` exits 0 and file is non-empty
    - `git log --oneline HEAD~1..HEAD` shows commit starting with "chore(02.5): fetch ForgeCode resource files"
    - That commit carries `Signed-off-by: Shafqat Ullah <shafqat@sourcevo.com>` (DCO)
    - `git status --short` outputs nothing (clean after commit)
  </acceptance_criteria>
  <done>All 9 missing resource file paths exist at repo root with non-zero content; committed with DCO signoff. vertex.json, shell-plugin/*, templates/*, commands/* all present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub raw CDN → working tree | Resource files fetched from antinomyhq/forgecode; untrusted network path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.5-01 | Tampering | `curl` fetch of resource files from GitHub | mitigate | Fetch URLs are pinned to exact commit SHA `022ecd994eaec30b519b13348c64ef314f825e21` via the GitHub raw URL scheme. SHA pinning prevents a CDN cache substitution from injecting different content. Commit message records the upstream SHA for audit trail. |
| T-2.5-02 | Denial of Service | `git revert --no-commit` on 24 commits | accept | Revert is a local git operation. Conflict risk is near-zero because the commits being reverted are forward-sequential on a clean branch. If git reports conflicts, halt and inspect before proceeding. |
</threat_model>

<verification>
After both tasks complete:
1. `git status --short` outputs nothing
2. `find crates/kay-core/src -name 'lib.rs' -path '*/forge_*/*' | wc -l` = 23
3. `ls shell-plugin/forge.theme.zsh shell-plugin/doctor.zsh shell-plugin/keyboard.zsh shell-plugin/forge.setup.zsh templates/forge-partial-tool-error-reflection.md commands/github-pr-description.md vertex.json` — all exit 0
4. `ls shell-plugin/lib/` — directory exists with files
5. `git log --oneline -3` shows two new commits: the revert commit and the resource-files fetch commit, both with DCO signoff
</verification>

<success_criteria>
- Clean working tree (git status empty)
- 23 forge_*/lib.rs files restored under crates/kay-core/src/ (Phase 1 import state)
- shell-plugin/, templates/, commands/, vertex.json exist at repo root
- Both commits carry Signed-off-by: DCO trailer
- No planning or doc files modified (only crates/kay-core/src/ source + new resource files)
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-kay-core-sub-crate-split/02.5-01-SUMMARY.md`
</output>
