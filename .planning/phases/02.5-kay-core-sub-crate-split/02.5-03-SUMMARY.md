# Plan 02.5-03 Summary — Waves 1..5 Sub-Crates

**Plan**: `.planning/phases/02.5-kay-core-sub-crate-split/02.5-03-PLAN.md`
**Commits**: `f217222` (Wave 1) → `da40ca0` (Wave 2) → `7e22cc5` (Wave 3) → `7f485bf` (Wave 4) → `5a567f9` (Wave 5)
**Status**: ✅ COMPLETE — `cargo check -p forge_api` (integration gate) passes

---

## Wave Completion

| Wave | Commit    | Crates                                              | Size     |
|------|-----------|-----------------------------------------------------|----------|
| 1    | `f217222` | forge_domain, forge_spinner, forge_tracker          | ~500 files |
| 2    | `da40ca0` | forge_fs, forge_snaps                               | ~40 files  |
| 3    | `7e22cc5` | forge_app, forge_services                           | ~248 files |
| 4    | `7f485bf` | forge_infra, forge_repo                             | ~123 files |
| 5    | `5a567f9` | forge_api (integration gate)                        | ~6 files   |

---

## Rule 3 Deviations Applied

All deviations auto-fixed to minimally resolve dep version/API conflicts, cross-referenced to upstream ForgeCode SHA `022ecd994eaec30b519b13348c64ef314f825e21`.

### Workspace-level (root `Cargo.toml`)

| Deviation | Before | After | Reason |
|-----------|--------|-------|--------|
| `nom` | `"7"` | `"8.0.0"` | forge_domain parser trait v7→v8 API changes |
| `derive_more` | `{ v=1, features=["full"] }` | `{ v=2.1.1, features=["from","display","debug","deref","as_ref","try_into"] }` | Deref name conflict in v1 full; v2 requires opt-in |
| `url` | `"2"` | `{ v="2", features=["serde"] }` | `Url: serde::Serialize` trait bound |
| `whoami` | `"1"` | `"2.1.0"` | v1 returned `String`; v2 returns `Result<String>` |
| `gray_matter` | `"0.2"` | `"0.3.2"` | `parse<T>()` API signature change (Result vs ParsedEntity) |
| `tonic` | `"0.12"` | `{ v="0.14.5", features=["tls-webpki-roots"] }` | `ClientTlsConfig::new().with_webpki_roots()` is 0.14 API |
| `prost` / `prost-types` | `"0.13"` | `"0.14.3"` | Align with tonic 0.14.5 |
| `google-cloud-auth` | `"0.17"` | `"1.8"` | `credentials::Builder` is 1.x API |
| `reqwest` | `{ v="0.13", features=["rustls","json","stream","gzip","brotli"] }` | `{ v="0.12.23", features=["json","rustls-tls","hickory-dns","http2","stream","gzip","brotli"], default-features=false }` | `.hickory_dns()` is 0.12 API; avoids rmcp/oauth2 multi-version conflict |
| `async-openai` | `"0.26"` | `{ v="0.34", features=["response-types"], default-features=false }` | `types::responses` module moved behind feature in 0.34 |
| `cacache` | `"13.1.0"` | `{ v="13.1.0", features=["tokio-runtime"], default-features=false }` | Match upstream feature set |
| *NEW* `rmcp` | — | `{ v="0.10", features=["client","transport-sse-client-reqwest","transport-child-process","transport-streamable-http-client-reqwest","auth"] }` | Added to workspace.dependencies; replaces single-consumer direct dep |
| *NEW* `machineid-rs` | — | `"1.2.4"` | Required by forge_tracker (upstream uses direct dep) |

### Single-consumer direct deps (not in workspace.dependencies)

- `forge_domain`: `eserde = { version = "0.1.7", features = ["json"] }`
- `forge_services`: `async-stream = "0.3"`
- `forge_repo`: `tonic-prost = "0.14.5"` (runtime), `tonic-prost-build = "0.14.5"` ([build-dependencies])

---

## Upstream Resource Fetches (Wave 4)

Fetched from `022ecd994eaec30b519b13348c64ef314f825e21`:
- `crates/forge_repo/build.rs` — `tonic_prost_build::compile_protos("proto/forge.proto")?;`
- `crates/forge_repo/proto/forge.proto` — 218 lines, full gRPC service definition

---

## Verification

All per-crate gates pass:
```bash
cargo check -p forge_fs        # Wave 2
cargo check -p forge_snaps     # Wave 2
cargo check -p forge_app       # Wave 3
cargo check -p forge_services  # Wave 3
cargo check -p forge_infra     # Wave 4 (SQLite bundled native compile)
cargo check -p forge_repo      # Wave 4 (build.rs proto codegen)
cargo check -p forge_api       # Wave 5 — INTEGRATION GATE
```

All 20 `forge_*` sub-crates now exist as independent workspace members. The only remaining kay-core sub-tree is `forge_main`, handled by Plan 02.5-04.

---

## Next: Plan 02.5-04

Convert `kay-core` into a thin aggregator crate and extract `forge_main` as the CLI entry crate. Drop `--exclude kay-core` from CI. Update NOTICE/ATTRIBUTIONS to reflect sub-crate attribution.
