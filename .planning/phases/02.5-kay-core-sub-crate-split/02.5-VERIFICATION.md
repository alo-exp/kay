---
phase: 02.5-kay-core-sub-crate-split
verified: 2026-04-20T01:40:00Z
status: passed
score: 8/8 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 7/8
  gaps_closed:
    - "No `--exclude kay-core` remains in .github/workflows/*.yml, CONTRIBUTING.md, docs/CICD.md (and related contributor-facing docs)"
  gaps_remaining: []
  regressions: []
  closure_commit: 23e4a6d3125d4b1dda93c8529484ddb3898abb03
---

# Phase 2.5: kay-core Sub-Crate Split Verification Report

**Phase Goal:** Split the kay-core mono-crate into 23 `forge_*` workspace sub-crates plus a thin `kay-core` aggregator; remove every `--exclude kay-core` carve-out from CI/docs; workspace builds cleanly against Rust 1.95; ForgeCode upstream SHA `022ecd9…` parity preserved.
**Verified:** 2026-04-20T01:40:00Z (re-verification)
**Status:** passed
**Re-verification:** Yes — after gap closure (commit `23e4a6d`)

## Re-Verification Summary

The initial verification (2026-04-20T01:18:35Z) found 7/8 truths verified, with one doc-only gap: `.github/pull_request_template.md:7` still contained an active `cargo clippy --workspace --exclude kay-core ...` instruction.

Commit `23e4a6d` ("ci: drop --exclude kay-core from PR template (phase 2.5 gap closure)") updates the checklist line to `cargo fmt --all -- --check` + `cargo clippy --workspace --all-targets --all-features -- -D warnings`, dropping both the exclusion and the superseded Phase 1 deferral reference.

Post-fix re-scan of `.github/`, `CONTRIBUTING.md`, `docs/`, and CI configs:

- `.github/` — **no** active `--exclude kay-core` occurrences
- `CONTRIBUTING.md:50` — historical prose ("now builds cleanly without `--exclude kay-core`"); documentation of the removal, not an active flag — acceptable
- `docs/CICD.md:14` — historical prose ("Phase 2.5 split kay-core into 23 `forge_*` sub-crates, removing the prior `--exclude kay-core` carve-out"); narrative, not an active flag — acceptable
- `.github/workflows/ci.yml` — remains clean (workspace-wide commands, no exclusion) — no regression

All 8 truths now verified; phase goal fully achieved.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `cargo check --workspace` passes with zero exclusions | ✓ VERIFIED | Initial verification: exit 0, `Finished dev profile target(s) in 1.43s`, no errors, no warnings |
| 2 | `cargo fmt --all -- --check` passes | ✓ VERIFIED | Initial verification: exit 0 (nightly-feature warnings only, harmless under stable 1.95) |
| 3 | All 23 `forge_*` sub-crates exist under `crates/` AND are declared as workspace members in root `Cargo.toml` | ✓ VERIFIED | `ls crates/ \| grep -E "^forge_"` returns exactly 23 directories; `Cargo.toml` members list (lines 6-38) includes all 23 |
| 4 | `crates/kay-core/src/lib.rs` is a thin aggregator (`pub extern crate forge_*`), not a mono-crate | ✓ VERIFIED | 20-line file re-exporting the 6 top-of-DAG sub-crates via `pub extern crate forge_api; forge_config; forge_domain; forge_json_repair; forge_repo; forge_services;` |
| 5 | No `--exclude kay-core` remains in `.github/workflows/*.yml`, `CONTRIBUTING.md`, `docs/CICD.md` | ✓ VERIFIED | **Gap closed by commit `23e4a6d`**: `.github/pull_request_template.md:7` now reads `cargo fmt --all -- --check` + `cargo clippy --workspace --all-targets --all-features -- -D warnings`. Repo-wide re-scan: no active `--exclude kay-core` in `.github/`, CI configs, or active doc instructions. Remaining `CONTRIBUTING.md:50` and `docs/CICD.md:14` mentions are historical narrative describing the removal — acceptable |
| 6 | `.forgecode-upstream-sha` contains `022ecd994eaec30b519b13348c64ef314f825e21` | ✓ VERIFIED | File content matches expected SHA |
| 7 | `tests/governance/check_attribution.sh` passes | ✓ VERIFIED | All 37 assertions PASS; exit 0 with `ALL GOVERNANCE INVARIANTS PASS` |
| 8 | `ATTRIBUTIONS.md` describes sub-crate layout (not old mono-crate layout) | ✓ VERIFIED | Lines 13-33 document the 23-crate sub-crate layout and Phase 2.5 as the divergence event |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `Cargo.toml` workspace members | 23 `forge_*` + `kay-core` + 7 other kay-* crates | ✓ VERIFIED | 31 members total |
| `crates/kay-core/src/lib.rs` | Aggregator re-exporter | ✓ VERIFIED | 20 lines, 6 `pub extern crate forge_*;` declarations |
| `crates/forge_*/` directories | 23 sub-crate roots | ✓ VERIFIED | Each with its own `Cargo.toml` and `src/` |
| `.forgecode-upstream-sha` | 40-char hex matching upstream | ✓ VERIFIED | Matches |
| `rust-toolchain.toml` | `channel = "1.95"` | ✓ VERIFIED | Pinned |
| `.github/workflows/ci.yml` | No `--exclude kay-core` | ✓ VERIFIED | Workspace-wide commands |
| `.github/pull_request_template.md` | No `--exclude kay-core` | ✓ VERIFIED | **Fixed in commit `23e4a6d`** — line 7 now uses `cargo fmt --all -- --check` + `cargo clippy --workspace --all-targets --all-features -- -D warnings` |
| `ATTRIBUTIONS.md` | Documents sub-crate layout | ✓ VERIFIED | Lines 13-33 describe split, reference Phase 2.5 |
| `tests/governance/check_attribution.sh` | Passes end-to-end | ✓ VERIFIED | All assertions pass |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Workspace compiles cleanly | `cargo check --workspace` | `Finished dev profile target(s) in 1.43s` (exit 0) | ✓ PASS |
| Workspace formatting is clean | `cargo fmt --all -- --check` | exit 0 | ✓ PASS |
| Governance invariants hold | `bash tests/governance/check_attribution.sh` | All 37 checks PASS; exit 0 | ✓ PASS |
| Sub-crate count matches contract | `ls crates/ \| grep -cE "^forge_"` | `23` | ✓ PASS |
| No active `--exclude kay-core` in contributor-facing docs | `grep -rn "\-\-exclude kay-core" .github/ CONTRIBUTING.md docs/` | Only historical narrative in `CONTRIBUTING.md:50` and `docs/CICD.md:14` (describing the removal) — no active instructions | ✓ PASS (re-verification) |

### Anti-Patterns Found

None. The prior ⚠️ Warning on `.github/pull_request_template.md:7` has been resolved by commit `23e4a6d`. No TODO/FIXME/placeholder markers in Phase 2.5-touched files. No stub code paths. No regressions.

### Gaps Summary

All Phase 2.5 success criteria now satisfied. The mono-crate has been split into 23 workspace sub-crates, `kay-core` is a thin 20-line aggregator, `cargo check --workspace` passes without any `--exclude` flag, governance checks pass, `ATTRIBUTIONS.md` documents the new layout, the ForgeCode upstream SHA is intact, and all active `--exclude kay-core` instructions have been removed from CI configs and contributor-facing docs (remaining mentions are historical prose documenting the removal).

**Phase 2.5 is complete. Ready to proceed.**

---

*Initial verification: 2026-04-20T01:18:35Z — status: gaps_found (7/8)*
*Re-verified: 2026-04-20T01:40:00Z — status: passed (8/8); gap closed by commit `23e4a6d`*
*Verifier: Claude (gsd-verifier)*
