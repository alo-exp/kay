---
phase: 2.5
slug: kay-core-sub-crate-split
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-20
---

# Phase 2.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | cargo (Rust native — check/test/clippy) |
| **Config file** | `rust-toolchain.toml` (workspace-level; no per-phase config needed) |
| **Quick run command** | `cargo check -p <name>` (per sub-crate; swap `<name>` for each) |
| **Full suite command** | `cargo check --workspace --deny warnings` |
| **Estimated runtime** | ~60–120s full workspace (diesel/SQLite native linkage adds time) |

---

## Sampling Rate

- **After every sub-crate creation:** Run `cargo check -p <new-crate-name>`
- **After every plan wave:** Run `cargo check -p kay-provider-openrouter --tests`
- **Before `/gsd-verify-work`:** Full suite (`cargo check --workspace --deny warnings`) must be green
- **Max feedback latency:** 120 seconds (bounded by diesel/SQLite compile)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 2.5-01-01 | 01 | 0 | WS-05 | — | N/A | cargo check | `git status --short \| grep -c '.'` = 0 after restore | ✅ W0 | ⬜ pending |
| 2.5-01-02 | 01 | 0 | WS-05 | — | N/A | cargo check | `git log --oneline HEAD~1` shows revert commit | ✅ W0 | ⬜ pending |
| 2.5-01-03 | 01 | 0 | WS-05 | — | N/A | file-exists | `ls shell-plugin/ templates/ commands/ vertex.json` exits 0 | ✅ W0 | ⬜ pending |
| 2.5-02-01 | 02 | 0 | WS-05 | — | N/A | cargo check | `cargo check -p forge_tool_macros` | ✅ W0 | ⬜ pending |
| 2.5-02-02 | 02 | 0 | WS-05 | — | N/A | cargo check | `cargo check -p forge_template` | ✅ W0 | ⬜ pending |
| 2.5-02-03 | 02 | 0 | WS-05 | — | N/A | cargo check | `cargo check -p forge_json_repair` | ✅ W0 | ⬜ pending |
| 2.5-02-04 | 02 | 0 | WS-05 | — | N/A | cargo check | `cargo check -p forge_stream forge_test_kit forge_embed forge_ci forge_walker forge_markdown_stream forge_select forge_display forge_config` | ✅ W0 | ⬜ pending |
| 2.5-03-01 | 03 | 1 | WS-05 | — | N/A | cargo check | `cargo check -p forge_domain` | ✅ W0 | ⬜ pending |
| 2.5-03-02 | 03 | 1 | WS-05 | — | N/A | cargo check | `cargo check -p forge_spinner forge_tracker forge_fs forge_snaps` | ✅ W0 | ⬜ pending |
| 2.5-03-03 | 03 | 2 | WS-05 | — | N/A | cargo check | `cargo check -p forge_app forge_services` | ✅ W0 | ⬜ pending |
| 2.5-03-04 | 03 | 3 | WS-05 | — | N/A | cargo check | `cargo check -p forge_infra forge_repo forge_api` | ✅ W0 | ⬜ pending |
| 2.5-04-01 | 04 | 4 | WS-05 | — | N/A | cargo check | `cargo check -p forge_main` | ✅ W0 | ⬜ pending |
| 2.5-04-02 | 04 | 4 | PROV-01 | — | N/A | cargo check | `cargo check -p kay-provider-openrouter --tests` | ✅ W0 | ⬜ pending |
| 2.5-04-03 | 04 | 4 | WS-05 | — | N/A | cargo check | `cargo check --workspace --deny warnings` (no --exclude) | ✅ W0 | ⬜ pending |
| 2.5-04-04 | 04 | 4 | WS-05 | — | N/A | grep | `grep -r 'exclude.*kay-core' .github/ CONTRIBUTING.md` returns empty | ✅ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

No new test files needed — Phase 2.5 is pure structural refactoring. The compilation gates above ARE the validation contract for this phase. All commands use the existing `cargo` toolchain already in the workspace.

Existing infrastructure covers all phase requirements:
- `crates/kay-provider-openrouter/tests/` (Wave 0 scaffold from plan 02-01) — unchanged

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `include_str!` resource files resolve at runtime | WS-05 | Compile-time `cargo check` catches missing files, but `cargo check` does not run code. Runtime test not needed until Phase 5 when forge_main binary is used. | Run `cargo build -p forge_main` (not just check) to catch `include_dir!` macro issues. |
| `forge_tool_macros` proc-macro derives work in forge_domain | WS-05 | `cargo check` compiles the proc-macro; `#[derive(ToolDescription)]` usage in forge_domain confirms the link works. | `cargo check -p forge_domain` — any derive-not-found error signals proc-macro isolation failure. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
