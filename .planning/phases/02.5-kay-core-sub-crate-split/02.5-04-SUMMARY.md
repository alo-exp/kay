# Plan 02.5-04 — Wave 6 + Aggregator + CI Cleanup — SUMMARY

**Status:** COMPLETE
**Executed:** 2026-04-20
**Commits (Plan 04 scope):** `64515b1`, `9d6a32a`, `3268b55`, `ca784e5`

Plan 04 closes Phase 2.5 by landing the terminal Wave (forge_main binary),
converting kay-core from a mono-crate into a thin aggregator re-exporter,
wiring kay-provider-openrouter to the new sub-crates directly, and finally
removing every `--exclude kay-core` carve-out across CI and docs.

---

## Tasks Executed

### Task 1 — Wave 6: forge_main (commit `64515b1`)

Created the terminal sub-crate of the forge_* DAG:

- `crates/forge_main/Cargo.toml` — CLI binary `[[bin]] name="forge" path="src/main.rs"`
- `crates/forge_main/build.rs` — upstream-verbatim version-string embedding
- Depends on forge_api and all user-facing layers (display, select, spinner, walker, template, markdown_stream, stream, tracker)

**Rule 3 deviations (workspace-level dep bumps) applied in this task:**

| Dep | Was | Became | Reason |
|---|---|---|---|
| `reedline` | `0.37` | `0.47.0` | `Signal::ExternalBreak` + `ReedlineRawEvent::try_from` ABI |
| `humantime` | `0.1` | `2.1.0` | `format_duration` API shape |
| `convert_case` | `0.6` | `0.11.0` | `Case::Sentence` variant added |
| `strum` | `0.27` (+derive feat) | `0.28.0` (derive removed; re-exported via strum_macros) | `EnumProperty` double-import diag |
| `strum_macros` | `0.27` | `0.28.0` | paired bump |
| `update-informer` | workspace dep (no features) | inline pin `default-features=false, features=["github","ureq","rustls-tls"]` | rustls-only build |

Gate: `cargo check -p forge_main` passed.

### Task 2 — kay-core aggregator conversion (commit `9d6a32a`)

- `crates/kay-core/src/lib.rs`: replaced 23 `pub mod forge_*` declarations
  (pointing to directories that no longer exist) with 6 `pub extern crate forge_*`
  re-exports for the top-of-DAG sub-crates: `forge_api`, `forge_config`,
  `forge_domain`, `forge_json_repair`, `forge_repo`, `forge_services`.
- `crates/kay-core/Cargo.toml`: path-deps on those same 6 sub-crates.
- `crates/kay-provider-openrouter/Cargo.toml`: `[dependencies]` populated with
  direct path-deps on `forge_domain`, `forge_config`, `forge_services`,
  `forge_repo`, `forge_json_repair` plus workspace runtime deps (tokio,
  reqwest, reqwest-eventsource, serde, serde_json, anyhow, thiserror,
  tracing). Dev-deps unchanged.

Gates: `cargo check -p kay-core` (2.08s), `cargo check -p kay-provider-openrouter --tests` (26.94s).

### Task 3 — CI/docs cleanup (commit `3268b55`)

Removed every `--exclude kay-core` carve-out introduced in Phase 1:

- `.github/workflows/ci.yml`: the explicit 7-package fmt list collapsed to
  `cargo fmt --all -- --check`; clippy and test steps dropped
  `--exclude kay-core`.
- `CONTRIBUTING.md`: PR pre-push instructions now say `cargo fmt --all` +
  `cargo clippy --workspace`.
- `docs/CICD.md`: `test` job line updated.
- `.planning/STATE.md`: Phase 2 structural-integration Blocker marked
  RESOLVED.
- `crates/forge_main/src/info.rs`: whitespace-only fix surfaced by
  `cargo fmt --all` (trailing space inside a doc comment).

Gate: `cargo check --workspace` (no exclusions) passed in 29.03s.

### Task 4 — Attribution update (commit `ca784e5`)

- `ATTRIBUTIONS.md` "Location in Kay" paragraph now enumerates all 23
  `forge_*` sub-crates and describes kay-core's re-exporter role.
- Added a bullet documenting the Phase 2.5 split and the workspace-level
  dep bumps required for Rust 1.95, cross-referencing
  `02.5-03-SUMMARY.md` §Rule 3 Deviations.
- `NOTICE` and `.forgecode-upstream-sha` unchanged (still pin
  `022ecd994eaec30b519b13348c64ef314f825e21`).
- `tests/governance/check_attribution.sh` passes (all invariants PASS).

---

## Exit State

- `cargo check --workspace` — PASS, 0 warnings, 0 errors, 0 exclusions.
- `cargo fmt --all -- --check` — PASS.
- Governance invariants — PASS.
- kay-core is now a ~20-line aggregator; the 23 forge_* sub-crates are
  the unit of import and versioning going forward.

## Phase 2.5 Closure

Plan 04 is the final plan in Phase 2.5. With all four plans merged the
phase goal — "make the imported ForgeCode source compile cleanly as a
workspace without mono-crate pathologies" — is achieved. Ready for
`/gsd-verify-work 2.5`.
