---
phase: 01-fork-governance-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE
  - NOTICE
  - README.md
  - CONTRIBUTING.md
  - SECURITY.md
  - CODE_OF_CONDUCT.md
  - ATTRIBUTIONS.md
  - docs/signing-keys/README.md
  - .github/pull_request_template.md
autonomous: true
requirements:
  - GOV-01
  - GOV-02
  - GOV-04
  - GOV-06
  - GOV-07
user_setup: []

must_haves:
  truths:
    - "Anyone can clone kay/ and see ForgeCode attribution in NOTICE, README Acknowledgments section, and (after plan 03) every crate's authors field"
    - "LICENSE at repo root is Apache-2.0 verbatim from apache.org"
    - "NOTICE lists Tailcall copyright (ForgeCode's actual copyright holder per D-02) plus Kay's own copyright"
    - "CONTRIBUTING.md documents DCO signoff AND the exact clean-room attestation clause citing '@anthropic-ai/claude-code v2.1.88' and '2026-03-31'"
    - "SECURITY.md describes the GitHub private Security Advisory flow + response SLA"
  artifacts:
    - path: "LICENSE"
      provides: "Apache-2.0 license text verbatim"
      contains: "Apache License"
    - path: "NOTICE"
      provides: "Apache §4(d) attribution"
      contains: "Tailcall"
    - path: "README.md"
      provides: "Project overview with ForgeCode + KIRA acknowledgments"
      contains: "## Acknowledgments"
    - path: "CONTRIBUTING.md"
      provides: "DCO + clean-room attestation + PR process"
      contains: "Developer Certificate of Origin"
    - path: "SECURITY.md"
      provides: "Vulnerability reporting flow + response SLA"
      contains: "Security Advisory"
    - path: "CODE_OF_CONDUCT.md"
      provides: "Contributor Covenant v2.1 for community health"
      contains: "Contributor Covenant"
    - path: "ATTRIBUTIONS.md"
      provides: "Upstream commit pointer + derived-file list (SHA filled in plan 03)"
      contains: "UPSTREAM_COMMIT"
    - path: ".github/pull_request_template.md"
      provides: "DCO reminder + clean-room checkbox in every PR"
      contains: "Signed-off-by"
  key_links:
    - from: "CONTRIBUTING.md"
      to: "SECURITY.md"
      via: "section reference"
      pattern: "SECURITY\\.md"
    - from: "CONTRIBUTING.md"
      to: "developercertificate.org"
      via: "DCO v1.1 link"
      pattern: "developercertificate\\.org"
    - from: "SECURITY.md"
      to: "docs/signing-keys/"
      via: "signing keys path"
      pattern: "docs/signing-keys"
---

<objective>
Author the eight governance artifacts Apache-2.0 §4 and the Kay non-negotiables require: LICENSE (verbatim Apache-2.0), NOTICE (Apache §4(d) attribution to Tailcall), README.md (with §Acknowledgments section), CONTRIBUTING.md (DCO + clean-room attestation), SECURITY.md (private-advisory flow + SLA), CODE_OF_CONDUCT.md (Contributor Covenant v2.1), ATTRIBUTIONS.md (upstream commit placeholder), plus docs/signing-keys/README.md and .github/pull_request_template.md.

Purpose: These files are the legal + social shield Kay needs before accepting any external contribution. Without them the Apache-2.0 grant is incomplete and the clean-room posture is unenforceable.
Output: Governance files committed at repo root ready for verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fork-governance-infrastructure/01-CONTEXT.md
@.planning/phases/01-fork-governance-infrastructure/01-RESEARCH.md
@.planning/phases/01-fork-governance-infrastructure/01-VALIDATION.md
@CLAUDE.md

<interfaces>
The concrete text for each file is specified below (copy verbatim; do not paraphrase).

**Clean-room attestation clause (per D-16, verbatim; do NOT reword):**

> "By signing off, I confirm I have not had exposure to leaked Claude Code source code (`@anthropic-ai/claude-code` v2.1.88 source map leak, 2026-03-31) and that this contribution contains no code derived from that leak."

The exact strings `@anthropic-ai/claude-code`, `v2.1.88`, and `2026-03-31` MUST appear in CONTRIBUTING.md.

**Apache-2.0 LICENSE source of truth:** https://www.apache.org/licenses/LICENSE-2.0.txt (fetch via curl/WebFetch; use the plain-text version verbatim).

**NOTICE content (from 01-RESEARCH.md §Example 1, verbatim):**

```
Kay
Copyright 2026 Kay Contributors

Portions of this product were derived from the ForgeCode project
(https://github.com/antinomyhq/forgecode), Copyright 2025 Tailcall,
licensed under the Apache License, Version 2.0.

Imported from ForgeCode at commit <SHA> on 2026-04-19.
Specific modifications made by Kay are listed in ATTRIBUTIONS.md.

This product makes use of dependencies with their own licenses.
See Cargo.lock and the output of `cargo-about generate` for a
complete manifest.
```

Leave `<SHA>` as the literal placeholder `<SHA>` — plan 03 (ForgeCode import) fills it in when the actual HEAD SHA is known.

**CONTRIBUTING.md content:** copy verbatim from 01-RESEARCH.md §Example 2.

**SECURITY.md content:** copy verbatim from 01-RESEARCH.md §Example 3.

**CODE_OF_CONDUCT.md:** use the Contributor Covenant v2.1 full text from https://www.contributor-covenant.org/version/2/1/code_of_conduct.txt (replace the `[INSERT CONTACT METHOD]` placeholder with `security@kay.dev` per SECURITY.md).

**.github/pull_request_template.md:**

```markdown
## Summary

<!-- Short description of the change. Link related issues. -->

## Checklist

- [ ] I have run `cargo fmt --all` and `cargo clippy --workspace --all-targets -- -D warnings` locally
- [ ] Every commit is signed off (`Signed-off-by: Name <email>`) — the DCO job will fail otherwise
- [ ] I have not had exposure to the `@anthropic-ai/claude-code` v2.1.88 leak (2026-03-31); this PR contains no leak-derived code (clean-room attestation per CONTRIBUTING.md)
- [ ] I have updated tests where behavior changes
- [ ] I have updated docs (README, CONTRIBUTING, SECURITY) if the public contract changes

## Related Requirements

<!-- Reference REQ-IDs from .planning/REQUIREMENTS.md (e.g., GOV-04, WS-01). -->
```

**ATTRIBUTIONS.md (initial scaffold — SHA filled in by plan 03):**

```markdown
# Attributions

This file lists third-party source code incorporated into Kay beyond what
`Cargo.lock` can express (because cargo's dependency graph doesn't cover
vendored source).

## ForgeCode (primary upstream)

- **Origin:** https://github.com/antinomyhq/forgecode
- **License:** Apache-2.0 (see LICENSE and NOTICE)
- **Upstream commit:** `<UPSTREAM_COMMIT>` (filled in by plan 03 when source is imported)
- **Import date:** 2026-04-19
- **Location in Kay:** `crates/kay-core/src/**` and any `crates/kay-core-*` if multi-crate structure is preserved
- **Deliberate divergences from upstream:**
  - `rust-toolchain.toml` pinned to `1.95` (ForgeCode pins `1.92`) — see 01-RESEARCH.md §Pitfall 3
  - Per-crate `authors` replaced with `["Kay Contributors <contributors@kay.dev>"]` per D-03
  - Any `.github/workflows/*.yml` from ForgeCode is NOT imported — Kay's CI workflow is separate
  - New files added by Kay: NOTICE (ForgeCode had none), CONTRIBUTING.md (DCO + clean-room), SECURITY.md, CODE_OF_CONDUCT.md (Contributor Covenant v2.1)
- **Purpose:** The unmodified ForgeCode harness is the parity baseline for Terminal-Bench 2.0 (EVAL-01). The `forgecode-parity-baseline` git tag anchors the import SHA.

## Kay's own copyright line policy

Kay is a contributor collective. The LICENSE appendix line reads
`Copyright 2026 Kay Contributors`. Individual contributions are tracked via
DCO `Signed-off-by` trailers in git history (see CONTRIBUTING.md).
```

**docs/signing-keys/README.md:**

```markdown
# Release Signing Keys

This directory publishes the GPG and/or SSH public keys used to sign Kay
release tags. Phase 1 ships no tags; the first signed tag lands in a later
phase (Phase 11 per the deferred procurement schedule in CONTEXT.md §User
Amendments — D-OP-04).

When a key is added here:

- Filename convention: `{handle}-{algorithm}.pub` (e.g. `shafqat-ssh.pub`,
  `shafqat-gpg.asc`).
- Keys are rotated annually or on compromise (per D-15 in 01-CONTEXT.md).
- SECURITY.md cross-references this directory.

Verify a release tag with:

    git tag -v vX.Y.Z
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write LICENSE, NOTICE, README, ATTRIBUTIONS, CODE_OF_CONDUCT, docs/signing-keys/README, .github/pull_request_template</name>
  <files>LICENSE, NOTICE, README.md, ATTRIBUTIONS.md, CODE_OF_CONDUCT.md, docs/signing-keys/README.md, .github/pull_request_template.md</files>
  <read_first>
    - .planning/phases/01-fork-governance-infrastructure/01-CONTEXT.md (D-01, D-02, D-03, D-04, D-15, D-16, D-17)
    - .planning/phases/01-fork-governance-infrastructure/01-RESEARCH.md §Example 1 (NOTICE), §ForgeCode-Inherited File Audit (LICENSE copyright line policy), §Don't Hand-Roll (template sources)
    - .planning/phases/01-fork-governance-infrastructure/01-VALIDATION.md (verification commands for GOV-01, GOV-02, GOV-06)
    - .planning/PROJECT.md (core value, repo URL)
    - LICENSE, NOTICE, README.md, ATTRIBUTIONS.md, CODE_OF_CONDUCT.md (none exist yet — confirm before writing)
  </read_first>
  <action>
Write seven files. Use `curl -fsSL {URL}` to fetch the upstream-canonical texts where specified — do not hand-type them.

1. **`LICENSE`** — Fetch the verbatim Apache-2.0 text:
   ```bash
   curl -fsSL https://www.apache.org/licenses/LICENSE-2.0.txt -o LICENSE
   ```
   After fetching, append ONE line to the end of the LICENSE file's appendix (the part that reads "Copyright [yyyy] [name of copyright owner]"). Replace the placeholder with:
   ```
      Copyright 2026 Kay Contributors
   ```
   The Apache-2.0 license TEXT itself is verbatim — do not modify the body. Only the appendix line changes. Per 01-RESEARCH.md §ForgeCode-Inherited File Audit, Kay's copyright goes in LICENSE's appendix; Tailcall's copyright goes in NOTICE (option 2 in the research).

2. **`NOTICE`** — Copy the exact text from the `<interfaces>` block verbatim. Keep the `<SHA>` placeholder literal (plan 03 fills it in). The file MUST be ≤ 20 lines per 01-RESEARCH.md §Pitfall 1.

3. **`README.md`** — Write a minimal but complete README. Required sections:
   - Title: `# Kay`
   - One-line tagline: `Open-source terminal coding agent — a Rust fork of ForgeCode, hardened with Terminus-KIRA harness techniques, and delivered through a native Tauri desktop UI.`
   - `## Status` — "Pre-1.0. Phase 1 (Fork, Governance, Infrastructure) in progress. See `.planning/ROADMAP.md`."
   - `## Acknowledgments` section (per D-04) — MUST name ForgeCode as the base harness AND Terminus-KIRA as the source of the harness techniques:
     ```markdown
     ## Acknowledgments

     Kay is a fork of [ForgeCode](https://github.com/antinomyhq/forgecode)
     (Copyright 2025 Tailcall, Apache-2.0) — Tailcall's ForgeCode is the
     base harness Kay builds on. See `NOTICE` and `ATTRIBUTIONS.md` for
     attribution details.

     Kay's harness techniques — multi-perspective verification, marker-based
     shell polling, schema hardening, tool registry design — are informed by
     [Terminus-KIRA](https://github.com/terminus-ai/KIRA), whose published
     approaches Kay re-implements from specification in clean-room fashion.
     ```
   - `## Contributing` — Brief pointer: "See `CONTRIBUTING.md` for the DCO + clean-room attestation workflow."
   - `## Security` — Brief pointer: "See `SECURITY.md` for vulnerability disclosure."
   - `## License` — "Apache-2.0. See `LICENSE` and `NOTICE`."

4. **`ATTRIBUTIONS.md`** — Copy verbatim from the `<interfaces>` block. Keep `<UPSTREAM_COMMIT>` as the literal placeholder — plan 03 replaces it.

5. **`CODE_OF_CONDUCT.md`** — Fetch Contributor Covenant v2.1:
   ```bash
   curl -fsSL https://www.contributor-covenant.org/version/2/1/code_of_conduct.txt -o CODE_OF_CONDUCT.md
   ```
   Then edit the `[INSERT CONTACT METHOD]` placeholder inside the downloaded file, replacing it with `security@kay.dev`. All other text stays verbatim.

6. **`docs/signing-keys/README.md`** — Copy verbatim from the `<interfaces>` block. Create the parent directory `docs/signing-keys/` first via `mkdir -p`.

7. **`.github/pull_request_template.md`** — Copy verbatim from the `<interfaces>` block. The file path is `.github/pull_request_template.md` (GitHub's canonical location).

Do NOT write CONTRIBUTING.md or SECURITY.md in this task — those are Task 2.
  </action>
  <verify>
    <automated>test -f LICENSE && grep -q 'Apache License' LICENSE && grep -q 'Copyright 2026 Kay Contributors' LICENSE && test -f NOTICE && grep -q 'Tailcall' NOTICE && grep -q 'antinomyhq/forgecode' NOTICE && grep -q '<SHA>' NOTICE && test -f README.md && grep -q '## Acknowledgments' README.md && grep -q 'ForgeCode' README.md && grep -q 'Terminus-KIRA' README.md && test -f ATTRIBUTIONS.md && grep -q 'UPSTREAM_COMMIT' ATTRIBUTIONS.md && test -f CODE_OF_CONDUCT.md && grep -q 'Contributor Covenant' CODE_OF_CONDUCT.md && grep -q 'security@kay.dev' CODE_OF_CONDUCT.md && test -f docs/signing-keys/README.md && test -f .github/pull_request_template.md && grep -q 'Signed-off-by' .github/pull_request_template.md && grep -q 'v2.1.88' .github/pull_request_template.md && [ "$(wc -l < NOTICE)" -le 20 ]</automated>
  </verify>
  <acceptance_criteria>
    - `LICENSE` exists and contains string `Apache License` (from Apache-2.0 header)
    - `LICENSE` contains string `Copyright 2026 Kay Contributors` (appendix line replacement)
    - `NOTICE` exists at repo root, is ≤ 20 lines (per Pitfall 1 brevity rule)
    - `NOTICE` contains string `Tailcall` (ForgeCode's verified copyright holder)
    - `NOTICE` contains string `antinomyhq/forgecode`
    - `NOTICE` contains the literal placeholder `<SHA>` (filled in plan 03)
    - `NOTICE` contains string `2026-04-19` (import date)
    - `README.md` contains `## Acknowledgments` heading
    - `README.md` contains `ForgeCode` and `Terminus-KIRA`
    - `ATTRIBUTIONS.md` contains the literal placeholder `<UPSTREAM_COMMIT>`
    - `ATTRIBUTIONS.md` contains string `rust-toolchain.toml pinned to \`1.95\``
    - `CODE_OF_CONDUCT.md` contains `Contributor Covenant` AND `security@kay.dev` (replacing the INSERT CONTACT METHOD placeholder — grep for `[INSERT CONTACT METHOD]` must return zero)
    - `docs/signing-keys/README.md` exists and references `git tag -v`
    - `.github/pull_request_template.md` exists
    - `.github/pull_request_template.md` contains string `Signed-off-by`
    - `.github/pull_request_template.md` contains string `v2.1.88`
    - `.github/pull_request_template.md` contains string `2026-03-31`
  </acceptance_criteria>
  <done>Apache-2.0 LICENSE + NOTICE + README Acknowledgments + ATTRIBUTIONS placeholder + COC + signing-keys/README + PR template committed. All satisfy §4(d) attribution + D-04 acknowledgments clause.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write CONTRIBUTING.md and SECURITY.md</name>
  <files>CONTRIBUTING.md, SECURITY.md</files>
  <read_first>
    - .planning/phases/01-fork-governance-infrastructure/01-CONTEXT.md (D-10, D-11, D-12, D-15, D-16, D-17)
    - .planning/phases/01-fork-governance-infrastructure/01-RESEARCH.md §Example 2 (CONTRIBUTING.md full text), §Example 3 (SECURITY.md full text), §Pitfall 2 (clean-room wording), §Pitfall 5 (first-time contributor guidance)
    - .planning/phases/01-fork-governance-infrastructure/01-VALIDATION.md (verification commands for GOV-04, GOV-06, GOV-07)
    - README.md (from Task 1 — confirm what it points to in SECURITY/CONTRIBUTING)
  </read_first>
  <action>
Write two governance files. Both texts are specified verbatim in 01-RESEARCH.md.

1. **`CONTRIBUTING.md`** — Copy verbatim from 01-RESEARCH.md §Example 2 (markdown block starting `# Contributing to Kay`). Do NOT paraphrase any section. Critical invariants that MUST survive copying:
   - The DCO section links to `https://developercertificate.org/` (not a mirror or paraphrase)
   - The `git commit -s` one-liner appears verbatim (per Pitfall 5)
   - The `git commit --amend -s` and `git rebase --signoff HEAD~3` recovery commands appear verbatim
   - The **Clean-Room Attestation** section contains the EXACT text from D-16:
     > "By signing off, I confirm I have not had exposure to leaked Claude Code source code (`@anthropic-ai/claude-code` v2.1.88 source map leak, 2026-03-31) and that this contribution contains no code derived from that leak."
   - The strings `v2.1.88`, `2026-03-31`, and `@anthropic-ai/claude-code` appear verbatim (per Pitfall 2 — anchoring on verifiable fact)
   - The Pull Request Process section lists: issue-first for non-trivial, one PR per logical change, `cargo fmt --all`, `cargo clippy --workspace --all-targets -- -D warnings`, CI pass requirement, Signed-off-by on every commit, clean-room fast-track
   - Links to `SECURITY.md` and `CODE_OF_CONDUCT.md`

2. **`SECURITY.md`** — Copy verbatim from 01-RESEARCH.md §Example 3 (markdown block starting `# Security Policy`). Critical invariants:
   - The `## Supported Versions` table lists `main / HEAD` as Yes and `Older tags` as No
   - The `## Reporting a Vulnerability` section starts with the bold **Do not open a public GitHub issue for a security concern.**
   - The primary channel is `https://github.com/alo-exp/kay/security/advisories` with "Report a vulnerability" step-by-step
   - The email fallback `security@kay.dev` appears verbatim
   - The `## Response SLA` section lists: Acknowledgment ≤ 72 hours, Assessment ≤ 7 days, Fix/mitigation ≤ 30 days critical / 90 days lower-severity
   - The `## Release Signing` section shows `git tag -v v0.1.0` and references `https://github.com/alo-exp/kay/tree/main/docs/signing-keys`
   - The `## Dependency Hygiene` section mentions `cargo-audit` + `cargo-deny` + RustSec
   - Attribution line naming OpenSSF `oss-vulnerability-guide`

Self-review note: after writing SECURITY.md, append a one-line footer: `> Phase 1 ships this attestation under self-review; legal review is encouraged before v1.` (per 01-VALIDATION.md §Manual-Only Verifications row "CONTRIBUTING.md legal text review").
  </action>
  <verify>
    <automated>test -f CONTRIBUTING.md && grep -q 'Developer Certificate of Origin' CONTRIBUTING.md && grep -q 'developercertificate.org' CONTRIBUTING.md && grep -q 'git commit -s' CONTRIBUTING.md && grep -q 'clean-room' CONTRIBUTING.md && grep -q '@anthropic-ai/claude-code' CONTRIBUTING.md && grep -q 'v2.1.88' CONTRIBUTING.md && grep -q '2026-03-31' CONTRIBUTING.md && grep -q 'SECURITY.md' CONTRIBUTING.md && test -f SECURITY.md && grep -q 'Security Advisory' SECURITY.md && grep -qE '(report|disclos)' SECURITY.md && grep -q 'security@kay.dev' SECURITY.md && grep -q 'git tag -v' SECURITY.md && grep -q 'docs/signing-keys' SECURITY.md && grep -q 'cargo-deny' SECURITY.md && grep -q 'cargo-audit' SECURITY.md && grep -q 'oss-vulnerability-guide' SECURITY.md</automated>
  </verify>
  <acceptance_criteria>
    - `CONTRIBUTING.md` exists at repo root
    - Contains `Developer Certificate of Origin` (exact string)
    - Contains `developercertificate.org` link
    - Contains `git commit -s` (literal shell command)
    - Contains `git commit --amend -s` and `git rebase --signoff HEAD~3`
    - Contains the clean-room attestation phrase `@anthropic-ai/claude-code` — verbatim per D-16
    - Contains the literal strings `v2.1.88` and `2026-03-31`
    - Contains cross-reference to `SECURITY.md`
    - Contains `cargo fmt --all` AND `cargo clippy --workspace --all-targets -- -D warnings`
    - Contains reference to `CODE_OF_CONDUCT.md`
    - `SECURITY.md` exists at repo root
    - Contains `Security Advisory` (GitHub's private flow)
    - Contains `security@kay.dev` (email fallback)
    - Contains `git tag -v` (signature verification example)
    - Contains reference to `docs/signing-keys`
    - Contains `cargo-deny` AND `cargo-audit` mentions
    - Contains attribution to `oss-vulnerability-guide` (OpenSSF template)
    - Contains SLA phrases `72 hours`, `7 days`, `30 days`, `90 days`
    - Contains self-review footer line `legal review is encouraged before v1`
  </acceptance_criteria>
  <done>CONTRIBUTING.md documents DCO + clean-room + PR process; SECURITY.md documents disclosure + SLA + signing-key pointer. GOV-04, GOV-06, GOV-07 artifacts in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External contributor → Kay repo | Unknown contributor submits PR; governance files are the legal + social filter before merge |
| Security researcher → maintainer | Vulnerability reporter needs a trusted private channel (GitHub Security Advisory + security@kay.dev fallback) |
| Release consumer → Kay binary | Downstream user verifies release tag signatures against published keys (signing-keys/README.md placeholder in P1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information disclosure | Clean-room attestation | mitigate | CONTRIBUTING.md clause verbatim from D-16 with exact leak version + date; legal-shield strength depends on specificity (per 01-RESEARCH.md Pitfall 2) |
| T-01-02 | Repudiation | DCO signoff on contributions | mitigate | CONTRIBUTING.md references DCO v1.1 at developercertificate.org; PR template adds checkbox; ci.yml DCO job enforces (GOV-03, plan 05) |
| T-01-03 | Information disclosure | Public security issue | mitigate | SECURITY.md explicitly forbids public issues; directs to GitHub Security Advisory (private) with email fallback |
| T-01-04 | Tampering | NOTICE file §4(d) compliance | mitigate | NOTICE kept ≤ 20 lines (Pitfall 1), lists actual copyright holder Tailcall (verified this session), no marketing prose, no fabricated endorsements |
| T-01-05 | Spoofing | Release signing keys not yet published | accept | docs/signing-keys/README.md is a placeholder in P1; first signed tag lands in Phase 11 per user amendment deferring D-OP-04 |
</threat_model>

<verification>
- `test -f LICENSE && test -f NOTICE && test -f README.md && test -f CONTRIBUTING.md && test -f SECURITY.md && test -f CODE_OF_CONDUCT.md && test -f ATTRIBUTIONS.md && test -f docs/signing-keys/README.md && test -f .github/pull_request_template.md` all return 0
- `grep -c 'ForgeCode' NOTICE README.md CONTRIBUTING.md` shows at least 1 match per file
- `grep -q 'v2.1.88' CONTRIBUTING.md && grep -q '2026-03-31' CONTRIBUTING.md` exits 0 (clean-room anchor)
- `wc -l < NOTICE` ≤ 20 (Pitfall 1 brevity rule)
</verification>

<success_criteria>
- [ ] GOV-01: NOTICE lists ForgeCode/Tailcall attribution; README has §Acknowledgments naming ForgeCode + KIRA; ATTRIBUTIONS.md has upstream-commit scaffold
- [ ] GOV-02: LICENSE is Apache-2.0 verbatim; NOTICE names copyright holder Tailcall
- [ ] GOV-04: CONTRIBUTING.md documents DCO + clean-room attestation + PR process + code style
- [ ] GOV-06: SECURITY.md describes GitHub Security Advisory flow + response SLA + signing-key pointer
- [ ] GOV-07: CONTRIBUTING.md clean-room clause cites v2.1.88 + 2026-03-31 verbatim per D-16
</success_criteria>

<output>
After completion, create `.planning/phases/01-fork-governance-infrastructure/01-02-SUMMARY.md` with:
- Files created with line counts
- grep output confirming critical strings (ForgeCode, Tailcall, v2.1.88, 2026-03-31, security@kay.dev)
- Note on the `<SHA>` / `<UPSTREAM_COMMIT>` placeholders that plan 03 will fill
- Any divergences from the verbatim research text (there should be none)
</output>
