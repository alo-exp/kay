# Parity Baseline — Run Deferred

The **import** of ForgeCode source is complete (plan 01-03, commit `8af1f2b`,
tag `forgecode-parity-baseline` → upstream SHA `022ecd994eaec30b519b13348c64ef314f825e21`).

The **parity RUN itself** — executing the unmodified ForgeCode harness against
Terminal-Bench 2.0 on an OpenRouter Exacto endpoint (Claude Opus 4.6 or GPT-5.4)
to reproduce ≥80% — is **deferred to follow-on task EVAL-01a**, per the
user's Phase 1 amendment (D-OP-01 → scaffold-only; see `01-CONTEXT.md`
§Operational Dependencies §User Amendments 2026-04-19).

EVAL-01a unblocks the first Phase 2 harness modification. Until the run
lands, the `forgecode-parity-baseline` tag anchors the import only, not a
reproduced score. The signing of this tag is likewise deferred to Phase 11
per D-OP-04.
