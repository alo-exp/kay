---
phase: 02-provider-hal-tolerant-json-parser
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - crates/kay-core/src/forge_api/lib.rs
  - crates/kay-core/src/forge_api/mod.rs
  - crates/kay-core/src/forge_app/lib.rs
  - crates/kay-core/src/forge_app/mod.rs
  - crates/kay-core/src/forge_ci/lib.rs
  - crates/kay-core/src/forge_ci/mod.rs
  - crates/kay-core/src/forge_config/lib.rs
  - crates/kay-core/src/forge_config/mod.rs
  - crates/kay-core/src/forge_display/lib.rs
  - crates/kay-core/src/forge_display/mod.rs
  - crates/kay-core/src/forge_domain/lib.rs
  - crates/kay-core/src/forge_domain/mod.rs
  - crates/kay-core/src/forge_embed/lib.rs
  - crates/kay-core/src/forge_embed/mod.rs
  - crates/kay-core/src/forge_fs/lib.rs
  - crates/kay-core/src/forge_fs/mod.rs
  - crates/kay-core/src/forge_infra/lib.rs
  - crates/kay-core/src/forge_infra/mod.rs
  - crates/kay-core/src/forge_json_repair/lib.rs
  - crates/kay-core/src/forge_json_repair/mod.rs
  - crates/kay-core/src/forge_main/lib.rs
  - crates/kay-core/src/forge_main/mod.rs
  - crates/kay-core/src/forge_markdown_stream/lib.rs
  - crates/kay-core/src/forge_markdown_stream/mod.rs
  - crates/kay-core/src/forge_repo/lib.rs
  - crates/kay-core/src/forge_repo/mod.rs
  - crates/kay-core/src/forge_select/lib.rs
  - crates/kay-core/src/forge_select/mod.rs
  - crates/kay-core/src/forge_services/lib.rs
  - crates/kay-core/src/forge_services/mod.rs
  - crates/kay-core/src/forge_snaps/lib.rs
  - crates/kay-core/src/forge_snaps/mod.rs
  - crates/kay-core/src/forge_spinner/lib.rs
  - crates/kay-core/src/forge_spinner/mod.rs
  - crates/kay-core/src/forge_stream/lib.rs
  - crates/kay-core/src/forge_stream/mod.rs
  - crates/kay-core/src/forge_template/lib.rs
  - crates/kay-core/src/forge_template/mod.rs
  - crates/kay-core/src/forge_test_kit/lib.rs
  - crates/kay-core/src/forge_test_kit/mod.rs
  - crates/kay-core/src/forge_tool_macros/lib.rs
  - crates/kay-core/src/forge_tool_macros/mod.rs
  - crates/kay-core/src/forge_tracker/lib.rs
  - crates/kay-core/src/forge_tracker/mod.rs
  - crates/kay-core/src/forge_walker/lib.rs
  - crates/kay-core/src/forge_walker/mod.rs
autonomous: true
requirements: [PROV-01]
must_haves:
  truths:
    - "All 23 forge_*/lib.rs files are renamed to mod.rs in a single commit"
    - "The 23 E0583 errors are resolved (module-declaration errors)"
    - "No source content inside any renamed file is modified — byte-identical content under new filename (parity-baseline integrity per D-01)"
    - "cargo check -p kay-core still reports errors (path-rewrite work lands in 02-03 through 02-05), but the specific E0583 errors are GONE"
  artifacts:
    - path: "crates/kay-core/src/forge_domain/mod.rs"
      provides: "renamed from forge_domain/lib.rs — contents unchanged"
      contains: "mod agent;"
    - path: "crates/kay-core/src/forge_json_repair/mod.rs"
      provides: "renamed from forge_json_repair/lib.rs — contents unchanged"
      contains: "mod error;"
  key_links:
    - from: "crates/kay-core/src/lib.rs"
      to: "crates/kay-core/src/forge_X/mod.rs (each of 23)"
      via: "pub mod forge_X; declaration in src/lib.rs"
      pattern: "pub mod forge_"
---

<objective>
Step 1 of D-01 per CONTEXT.md: rename `crates/kay-core/src/forge_X/lib.rs` to `crates/kay-core/src/forge_X/mod.rs` for all 23 forge_* subdirectories. Exactly one commit. No content edits inside any file.

Why one commit: the E0583 error class is binary — either the module system can resolve `pub mod forge_X` (22+ modules still broken) or it cannot. A partial rename creates a confused intermediate state where cargo reports a mix of E0583 and the downstream E0432/E0433 import errors, making bisect useless. RESEARCH §Pitfall 1 (lines 458-482) is explicit: Step 1 is atomic.

Why this plan does not touch any content: preserving byte-identical content under a different filename is the entire point of D-01 — it preserves the `forgecode-parity-baseline` tag's semantic integrity. The logical imports do not change in this plan; their PATH SYNTAX will be rewritten in plans 02-03 / 02-04 / 02-05.

After this plan: `cargo check -p kay-core` STILL reports errors (hundreds of E0432/E0433 unresolved-path errors surface once E0583 clears — RESEARCH §Pitfall 1 "Warning signs" lines 480-482). That is expected; plans 02-03 through 02-05 close those.

Output: 23 files renamed, 0 content edits, 1 commit. `cargo check -p kay-core 2>&1 | grep -c E0583` returns 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-provider-hal-tolerant-json-parser/02-CONTEXT.md
@.planning/phases/02-provider-hal-tolerant-json-parser/02-RESEARCH.md
@.planning/phases/02-provider-hal-tolerant-json-parser/02-PATTERNS.md
@.planning/phases/01-fork-governance-infrastructure/VERIFICATION.md
@crates/kay-core/src/lib.rs

<interfaces>
<!-- Exact 23 subtree list — derived from `ls crates/kay-core/src/forge_*/lib.rs`. Do NOT add or remove entries. -->

```
crates/kay-core/src/forge_api/lib.rs
crates/kay-core/src/forge_app/lib.rs
crates/kay-core/src/forge_ci/lib.rs
crates/kay-core/src/forge_config/lib.rs
crates/kay-core/src/forge_display/lib.rs
crates/kay-core/src/forge_domain/lib.rs
crates/kay-core/src/forge_embed/lib.rs
crates/kay-core/src/forge_fs/lib.rs
crates/kay-core/src/forge_infra/lib.rs
crates/kay-core/src/forge_json_repair/lib.rs
crates/kay-core/src/forge_main/lib.rs
crates/kay-core/src/forge_markdown_stream/lib.rs
crates/kay-core/src/forge_repo/lib.rs
crates/kay-core/src/forge_select/lib.rs
crates/kay-core/src/forge_services/lib.rs
crates/kay-core/src/forge_snaps/lib.rs
crates/kay-core/src/forge_spinner/lib.rs
crates/kay-core/src/forge_stream/lib.rs
crates/kay-core/src/forge_template/lib.rs
crates/kay-core/src/forge_test_kit/lib.rs
crates/kay-core/src/forge_tool_macros/lib.rs
crates/kay-core/src/forge_tracker/lib.rs
crates/kay-core/src/forge_walker/lib.rs
```

<!-- Top-level declaration in `crates/kay-core/src/lib.rs` (lines 19-42) — DO NOT modify this file. It already declares each forge_X with `pub mod forge_X;`. After Rust's module resolution, `pub mod forge_X` in parent module + `forge_X/mod.rs` is the canonical way to register a subdirectory module. -->

```rust
// crates/kay-core/src/lib.rs (already in place — no edit needed)
pub mod forge_api;
pub mod forge_app;
// ... 21 more identical declarations
pub mod forge_walker;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atomically rename all 23 forge_*/lib.rs to forge_*/mod.rs via git mv</name>
  <files>23 × crates/kay-core/src/forge_*/lib.rs → crates/kay-core/src/forge_*/mod.rs (see files_modified frontmatter for the full list)</files>
  <read_first>
    - crates/kay-core/src/lib.rs (confirm lines 20-42 list exactly 23 pub mod forge_X; declarations; the set of 23 subtrees listed in the interfaces block above MUST match this file's declarations one-for-one)
    - crates/kay-core/src/forge_json_repair/lib.rs (canonical example of current content — MUST NOT CHANGE)
    - crates/kay-core/src/forge_domain/lib.rs (canonical example — MUST NOT CHANGE)
    - crates/kay-core/src/forge_services/lib.rs (canonical example — MUST NOT CHANGE)
    - .planning/phases/02-provider-hal-tolerant-json-parser/02-RESEARCH.md §Pitfall 1 (lines 458-482) — this is the ONE BIG rename; downstream path rewrites come in later plans
    - .planning/phases/02-provider-hal-tolerant-json-parser/02-PATTERNS.md §Tier 1 Meta-Pattern (lines 85-159) — concrete transformation steps
  </read_first>
  <action>
  Perform 23 `git mv` operations in a single shell invocation so that git records the rename atomically. Content is byte-identical before and after — verify with `git log --stat` after the commit that renames show a single "renamed from/to" line each and NO line-count delta.

  Exact commands:

  ```bash
  cd /Users/shafqat/Documents/Projects/opencode/vs-others
  for sub in forge_api forge_app forge_ci forge_config forge_display forge_domain \
             forge_embed forge_fs forge_infra forge_json_repair forge_main \
             forge_markdown_stream forge_repo forge_select forge_services \
             forge_snaps forge_spinner forge_stream forge_template \
             forge_test_kit forge_tool_macros forge_tracker forge_walker; do
    git mv "crates/kay-core/src/${sub}/lib.rs" "crates/kay-core/src/${sub}/mod.rs"
  done
  ```

  After the 23 `git mv`, verify the invariants (do these verification steps INSIDE this task before committing — if any fail, `git reset HEAD` and diagnose):

  1. Zero `lib.rs` remain under `forge_*/`:
     ```bash
     ls crates/kay-core/src/forge_*/lib.rs 2>&1 | grep -v "No such file" | wc -l   # expect 0
     ```
  2. Exactly 23 `mod.rs` exist under `forge_*/`:
     ```bash
     ls crates/kay-core/src/forge_*/mod.rs | wc -l   # expect 23
     ```
  3. Git sees all 23 as renames, not delete+add:
     ```bash
     git status --short | grep -E "^R" | wc -l   # expect 23
     ```
  4. The E0583 error count drops to 0:
     ```bash
     cargo check -p kay-core 2>&1 | grep -c "E0583"   # expect 0
     ```
  5. But `cargo check -p kay-core` still reports OTHER errors (hundreds of E0432/E0433 path errors — this is EXPECTED per RESEARCH §Pitfall 1; plans 02-03 through 02-05 close these):
     ```bash
     cargo check -p kay-core 2>&1 | grep -cE "E04(32|33)"   # expect > 0
     ```

  If (1)-(4) all hold, commit:

  ```bash
  git commit -s -m "refactor(02-02): rename 23 forge_*/lib.rs to mod.rs

  D-01 Step 1 per .planning/phases/02-provider-hal-tolerant-json-parser/02-CONTEXT.md.
  Resolves the 23 E0583 module-declaration errors in kay-core. Source content
  byte-identical under new filename — preserves forgecode-parity-baseline tag
  integrity.

  Path rewrites (crate::X -> crate::forge_X::Y, forge_Y::Z -> crate::forge_Y::Z)
  land in plans 02-03, 02-04, 02-05.

  Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
  ```

  Do NOT:
  - Modify the top-level `crates/kay-core/src/lib.rs` — it already has `pub mod forge_X;` declarations that work once the module files are at `forge_X/mod.rs`.
  - Touch any content inside any renamed file. If `git status` reports anything other than 23 renames, abort and investigate.
  - Batch this with path rewrites — that is the explicit topic of 02-03/04/05 and belongs in per-wave commits for bisect clarity.
  - Run `cargo clean` — unnecessary and would slow subsequent wave verification.
  </action>
  <verify>
    <automated>test $(ls crates/kay-core/src/forge_*/lib.rs 2>/dev/null | wc -l) -eq 0 && test $(ls crates/kay-core/src/forge_*/mod.rs 2>/dev/null | wc -l) -eq 23 && test $(git log -1 --stat 2>/dev/null | grep -c "renamed from") -eq 23 && test $(cargo check -p kay-core 2>&1 | grep -c "E0583") -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `ls crates/kay-core/src/forge_*/lib.rs 2>/dev/null | wc -l` returns exactly 0 (no lib.rs remains under any forge_* subdir)
    - `ls crates/kay-core/src/forge_*/mod.rs | wc -l` returns exactly 23
    - `git log -1 --stat | grep -c "renamed from"` returns exactly 23 (the single commit records 23 renames)
    - `git log -1 --stat | grep -E "^ [^|]+ \| +[0-9]+ [+-]" | wc -l` returns exactly 0 (no content delta in any renamed file — pure rename, zero line changes)
    - `cargo check -p kay-core 2>&1 | grep -c "E0583"` returns exactly 0 (the 23 module-declaration errors are resolved)
    - `cargo check -p kay-core 2>&1 | grep -cE "(E0432|E0433)"` returns a value > 0 (unresolved imports are expected per RESEARCH §Pitfall 1 — closed in later plans)
    - `cargo check --workspace --exclude kay-core` continues to pass (Phase 1 baseline preserved — our rename only touched kay-core)
    - `cargo check -p kay-provider-openrouter --tests` continues to pass (plan 02-01 non-regression)
    - The commit message starts with `refactor(02-02):` AND contains `Signed-off-by:` AND contains `D-01 Step 1`
  </acceptance_criteria>
  <done>23 forge_*/lib.rs atomically renamed to mod.rs in one signed-off commit. Zero content edits. E0583 count is 0; remaining E0432/E0433 errors in kay-core are expected and addressed in plans 02-03 through 02-05. kay-provider-openrouter tests unaffected.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries (Phase 2, scoped to this plan)

| Boundary | Description |
|----------|-------------|
| git working tree → commit | Rename recorded as git rename (100% similarity) — no content tampering |
| kay-core source tree → downstream consumers | Parity-baseline tag `forgecode-parity-baseline` MUST remain semantically equivalent to pre-rename tree; any accidental content edit here would corrupt EVAL-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | forge_*/mod.rs content | mitigate | Acceptance criterion asserts `git log -1 --stat` shows zero line changes on any renamed file — a pure rename. Any non-zero diff fails verify. |
| T-02-02-02 | Tampering | parity-baseline tag semantic integrity | accept | The tag points at commit `8af1f2b` (pre-rename). This plan produces a new commit with renamed files. The tag is unaffected; it captures the "as-imported" snapshot per CONTEXT.md D-01 note. |
| T-02-02-03 | Elevation of Privilege (supply-chain proxy) | DCO signoff on the rename commit | mitigate | Commit message MUST include `Signed-off-by:` per CLAUDE.md NN-3; the ci.yml `dco` job blocks PRs missing this on ANY commit. |

</threat_model>

<verification>

Plan-level verification (run at the end of Wave 2):

```bash
# (a) 23 renames, zero content changes
test $(git log -1 --stat | grep -c "renamed from") -eq 23
test $(git log -1 --stat | grep -E "^ [^|]+ \| +[0-9]+ [+-]" | wc -l) -eq 0

# (b) E0583 resolved
test $(cargo check -p kay-core 2>&1 | grep -c "E0583") -eq 0

# (c) remaining E0432/E0433 errors are the expected pre-rewrite surface (plans 02-03 through 02-05 close)
cargo check -p kay-core 2>&1 | grep -cE "(E0432|E0433)"   # > 0 is expected and acceptable

# (d) non-regression — existing workspace minus kay-core still builds clean
cargo check --workspace --exclude kay-core

# (e) Wave 1 plan 02-01 artifacts unaffected
cargo check -p kay-provider-openrouter --tests
```

</verification>

<success_criteria>

- Exactly 23 `forge_*/mod.rs` files exist; zero `forge_*/lib.rs` remain
- Single commit recording 23 renames (`git log -1 --stat | grep "renamed from" | wc -l` == 23)
- Zero content edits (`git log -1 --stat | grep -E "^ [^|]+ \| +[0-9]+ [+-]" | wc -l` == 0)
- Commit message starts with `refactor(02-02):` and contains `Signed-off-by:`
- `cargo check -p kay-core 2>&1 | grep -c "E0583"` returns 0
- `cargo check --workspace --exclude kay-core` still passes (Phase 1 baseline)
- `cargo check -p kay-provider-openrouter --tests` still passes (Plan 02-01 baseline)

</success_criteria>

<output>
After completion, create `.planning/phases/02-provider-hal-tolerant-json-parser/02-02-SUMMARY.md` per the standard summary template. Record the remaining E0432/E0433 error count in the summary (expected to be in the low-thousands range per RESEARCH §Finding); this is the baseline for plans 02-03, 02-04, and 02-05 to drive toward 0.
</output>
