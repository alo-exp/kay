{{consecutive_calls}} consecutive tool calls with the same tool and arguments were detected. This often indicates a loop where the model is repeatedly calling the same tool without making progress.

**Recommended actions:**
1. Verify the tool's arguments are correct
2. Try an alternative approach or different tool
3. If you're genuinely waiting for an external change, pause and check the current state before continuing